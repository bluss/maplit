[package]
name = "rust2015user"
version = "0.1.0"
authors = ["bluss"]
publish = false
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
maplit = { version = "*", path = ".." }
